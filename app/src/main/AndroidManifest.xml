<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.hosttech.schulio">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Schulio">
        <activity
            android:name=".activity.singleMark"
            android:exported="false" />

        <service
            android:name=".service.AverageService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".activity.addMark"
            android:exported="false" />
        <activity
            android:name=".activity.addSubject"
            android:exported="false" />
        <activity
            android:name=".activity.singleSubject"
            android:exported="false" />
        <activity
            android:name=".activity.CreateNotification"
            android:exported="false" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>